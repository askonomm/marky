{
  "name": "marky",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "compile": "npx tsc",
    "start": "concurrently --kill-others \"npx tsc -w\" \"nodemon dist/main.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^22.1.0",
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.4",
    "typescript": "^5.5.4"
  }
}
